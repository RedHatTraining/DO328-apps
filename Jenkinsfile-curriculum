pipeline {
    agent {
        label 'master'
    }

    stages {

        stage("Install") {

            agent {
                docker {
                    image 'node:14'
                }
            }

            steps {
                echo "Branch is ${env.BRANCH_NAME}..."
                sh 'npm ci'
            }
        }

    }
}
